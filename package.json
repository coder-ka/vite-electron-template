{
  "name": "vite-electron-builder-react",
  "version": "0.0.1",
  "workspaces": [
    "electron-app",
    "page"
  ],
  "scripts": {
    "watch": "npm run watch -w electron-app",
    "build": "npm run build -w electron-app"
  }
}
